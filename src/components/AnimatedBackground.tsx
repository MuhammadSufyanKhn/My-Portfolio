// AnimatedBackground — intentionally minimal for the Warm Paper theme.
// The dot-grid texture and decorative rings are handled via CSS (index.css).
// This component is kept for forward compatibility but renders nothing.
export default function AnimatedBackground() {
  return null;
}
